{
    "name": "cypress-project",
    "version": "1.0.0",
    "scripts": {
        "test": "cypress run",
        "test:open": "cypress open",
        "test:api" : "cypress run --spec \"cypress/e2e/api/**/*.cy.js\""
    },
    "devDependencies": {
        "cypress": "^13.0.0"
    }
}